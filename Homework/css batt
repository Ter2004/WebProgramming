<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #2F5A76;
    }

    .flag {
      position: relative;
      width: 400px; /* Width */
      height: 300px; /* Height */
      background-color: #2F5A76;
      overflow: hidden;
    }

    .diagonal-left {
      position: absolute;
      width: 200%; /* Increased width to ensure the diagonal covers */
      height: 100px; /* Thicker bars */
      background-color: #F5BB60;
      top: 50%;
      left: -50%;
      transform: rotate(45deg);
      transform-origin: center;
    }

    .diagonal-right {
      position: absolute;
      width: 200%;
      height: 100px; /* Thicker bars */
      background-color: #F5BB60;
      top: 50%;
      left: -50%;
      transform: rotate(-45deg);
      transform-origin: center;
    }
  </style>
  <title>CSS Battle Design</title>
</head>
<body>
  <div class="flag">
    <div class="diagonal-left"></div>
    <div class="diagonal-right"></div>
  </div>
</body>
</html>
